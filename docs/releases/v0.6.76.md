# v0.6.76 - Phase 75 Atomic Mentions Integration Hotfix

This release hardens the Phase 75 atomic mention and channel AI reply implementation after Codex integration review.

## Fixed

- Replaced the full typed `@...` trigger range when selecting a mention chip.
- Preserved atomic Backspace behavior by avoiding an auto-inserted spacer after user mention atoms.
- Prevented AI-authored channel messages from triggering additional AI-to-AI reply chains.
- Added final stream `message_id` correlation to `channel.ai.stream.chunk`.
- Normalized `/ask` command content across bare command, repeated whitespace, and direct question callers.
- Merged structured streamed tool-call deltas by id while keeping a plain-text fallback.
- Normalized missing Web `user_type` values to canonical `human`.

## Verified

- `apps/api`: `go test ./internal/handlers -run 'TestPhase75|TestPhase65CAISlashCommandAsk|TestCreateMessage|TestCreateDMMessage' -count=1`
- `apps/web`: `pnpm exec tsc --noEmit`
- `apps/web`: `pnpm lint`

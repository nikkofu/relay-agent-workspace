# Release v0.5.55

## Summary

This release completes the CI dependency declaration needed by the Next.js 16 flat ESLint config.

## Delivered

- direct `@typescript-eslint/eslint-plugin` devDependency in `apps/web/package.json`
- direct `@typescript-eslint/parser` devDependency in `apps/web/package.json`
- workspace package version bump to `0.5.55`
- retained Phase 30 APIs from `v0.5.53`:
  - richer status controls
  - user-group membership and mention lookup
  - file retention and audit
  - workflow run detail, cancel, and retry

## Verification

- `cd apps/api && go test ./...`
- `cd apps/api && GOCACHE=$(pwd)/.cache/go-build go build ./...`
- `pnpm --filter relay-agent-workspace lint`
- `pnpm build` still hangs after `Creating an optimized production build ...` in this environment

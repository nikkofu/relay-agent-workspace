# v0.5.1 Release Notes

## Summary

This release starts Phase 10 by shipping the first Slack-parity collaboration APIs:

- channel members
- workspace invites
- channel topic, purpose, and archive state

It also formalizes the Phase 10 documentation flow so future backend and frontend waves are tracked consistently.

## API Surface

- `GET /api/v1/channels/:id/members`
- `POST /api/v1/channels/:id/members`
- `DELETE /api/v1/channels/:id/members/:userId`
- `PATCH /api/v1/channels/:id`
- `GET /api/v1/workspaces/:id/invites`
- `POST /api/v1/workspaces/:id/invites`

## Data Model

- `channel_members`
- `workspace_invites`
- `channels.topic`
- `channels.purpose`
- `channels.is_archived`

## Collaboration Handoff

### For Gemini

- build the channel members panel from `GET /api/v1/channels/:id/members`
- wire add/remove member actions
- build invite flow from `GET/POST /api/v1/workspaces/:id/invites`
- add topic and purpose editing around `PATCH /api/v1/channels/:id`

### For Nikko

- this release establishes the first explicit Slack-parity wave
- next planning candidates:
  - inbox and mentions
  - drafts
  - presence and typing

## Verification Checklist

- `cd apps/api && go test ./...`
- `cd apps/api && GOCACHE=$(pwd)/.cache/go-build go build ./...`
- `pnpm build`
